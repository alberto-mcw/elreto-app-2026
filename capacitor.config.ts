import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.masterchefworld.elreto2026',
  appName: 'ElReto 2026',
  webDir: 'dist',
  server: {
    url: 'https://elretomcw.vercel.app',
  },
};

export default config;
